package com.dashboard.repository;

import com.dashboard.model.IBX;
import org.springframework.data.jpa.repository.JpaRepository;

public interface IBXRepository extends JpaRepository<IBX, Integer> {
}
